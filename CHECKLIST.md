# 검증 체크리스트 — 69개 실험에서 실제로 밟은 스물두 가지

이 저장소는 로봇 실험 69개지만, 반복해서 값이 나간 것은 로봇 지식이 아니라 **자기 결과를 의심하는
절차**였다. 그 절차를 도메인에서 떼어내 한 장으로 모은 것이다.

각 항목은 셋으로 되어 있다 — **스스로에게 던지는 질문**, **그 규칙을 만든 실제 실패**(실험 번호),
**어떤 일에 붙는지**. 로봇이 아니어도 붙는다: 회귀 테스트, 벤치마크, 모델 평가, 계측·센서 검증,
파라미터 식별, A/B 비교, 안전 검증 문서.

> 쓰는 법: 결과가 나왔을 때 **믿기 전에** 훑는다. 22개를 다 볼 필요는 없고, 지금 하는 일이
> 어느 단계인지만 보면 된다.

## ⚠️ 저자가 나중에 다시 어긴 항목

같은 규칙을 두 번 어겼다는 건 그 규칙이 **문서에 있으면 안 되고 코드에 있어야 한다**는 신호다.
아래 표시된 셋은 이 저장소에서 **만든 사람이 직접 재위반**했다.

| 항목 | 만든 곳 | 다시 어긴 곳 |
|---|---|---|
| **3-1 실패할 수 있는 시험** | #56 (34.8 mm에서 멈춤) | #62(깊이 34.7 mm), #64(AUROC 1.00) |
| **5-4 손잡이를 하나씩만** | #67 (격자로 지형을 봄) | #68(그 격자에도 축이 하나 빠져 있었다) |
| **2-7 바닥인가 창인가** | #68 (초과를 못 보는 바) | #69(그 바가 시스템에 맞춰져 있었다) |
| **4-1 짝지어 비교할 것** | #59 | #62(결론 부호가 뒤집힐 뻔) · #66(깊이를 안 맞춘 39% 개선) |

셋 다 **문서에 적혀 있었고, 적은 사람이 다시 어겼다.** 그래서 5-3이 있다.

---

## 1. 모델과 관측을 정할 때

### 1-1. 데이터가 그 값을 **흔들어 줬나?**
- **실패**: IMU 바이어스·SLAM 헤딩·팔 마찰·심부 변형 모드까지 **다섯 번** 반복. 스틱션은
  저속 구간에서만 켜지는데 여기 궤적이 그 구간을 스쳐 지나가서 식별이 실패했다(#43·#46).
- **붙는 자리**: 파라미터 식별, 캘리브레이션, 로그에서 값 역산.
- **관측되지 않는 것은 추정되지 않는다.** 추정기를 고칠 게 아니라 **실험을 고쳐야** 한다.

### 1-2. 이 모델이 그 현상을 **표현하기는 하나?**
- **실패**: 두 정책이 구분이 안 돼서 "차이 없음"으로 적을 뻔했는데, 모델에 해당 물리가
  아예 없어서였다(#59, 0.12 vs 1.62 N — 14배). **표현력 없는 모델에서의 '차이 없음'은
  결과가 아니라 침묵이다.**
- **붙는 자리**: 시뮬레이션 기반 비교, 정책/설정 A·B, "영향 없음" 결론 전부.

### 1-3. 축소 모델이 **질문 자체를 지우지** 않았나?
- **실패**: 도구를 점으로 두면 "몸통이 금지구역을 지나는 각도"라는 질문이 존재하지 않는다(#45·#48).
- **붙는 자리**: 1-DOF/저차원 근사, "일단 단순하게" 프로토타입.

### 1-4. 그 정규화·사전지식은 **개선인가 가정 구매인가?**
- **실패**: 좁은 노출에서 5.5배를 벌어준 건 알고리즘이 아니라 가정 하나였고, 같은 자리에서
  "물리 기반" 모델은 격자를 촘촘히 할수록 **나빠졌다**(#51). **더 정확히 푸는 것과 옳은 것을
  푸는 것은 다르다.**
- **덧**: 그 가정이 맞는지는 **가정이 적용되는 데이터로는 확인할 수 없다**(#52). 확인하려면
  그 실패 모드를 실제로 볼 수 있는 관측을 새로 가져와야 한다 — 1-1과 같은 말이다.
- **붙는 자리**: 정규화 항, 사전분포, 도메인 지식 주입, 결측 보간.

---

## 2. 지표를 정할 때

### 2-1. **위해를 끈 조건**에서 그 지표가 0에 가깝나?
- **실패**: 힘 지표를 재는데 **자극을 완전히 끈 대조군**에서 2.17 N이 나왔고, 대상 물성을
  **16배**로 쓸어도 값이 같았다 — 대상이 아니라 **제어기의 정착 과도현상**을 재고 있었다(#61).
  대조군 한 번이면 드러나는데 이전 실험이 안 돌렸다.
- **붙는 자리**: 새 품질지표 도입, 블랭크/음성 대조, 베이스라인 정의.

### 2-2. 지표들이 **같은 축** 위에 있지 않나?
- **실패**: 위해 지표를 세 번 갈았는데(증분→진폭→누적) **셋 다 힘이었다.** 힘끼리 ρ=0.48~0.81,
  다른 축(변형)과는 −0.23~0.15(#63). **지표군 안의 일치는 정확성의 증거가 아니라 공통 축의
  증거다** — 축이 틀리면 셋이 나란히 틀리고 서로 검산해도 통과한다.
- **붙는 자리**: 지표 대시보드, 복합 스코어, "여러 지표가 다 좋다"는 보고.
- **결론에는 축을 붙여 말한다**: "X는 무의미하다"가 아니라 "**힘 축에서** X는 무의미하다".

### 2-3. **"안 보인다"를 숫자로** 말했나?
- **실패**: 표에는 27% 상승이 찍혀 있는데 본문은 "흔적을 전혀 안 남긴다"로 적었고, 그 **문장
  하나가 아홉 실험 동안 "새 검출기가 필요하다"를 끌고 갔다.** 검출기로 채점해 보니 기존 지표가
  AUROC 0.94, 새로 제안한 대체물이 0.76이었다(#64).
- **덧**: **둔감한 것**과 **눈먼 것**은 다른 주장이고, 뒤엣것만 대체물을 찾을 근거가 된다.
  같은 문장에 **고칠 수 없다**와 **감지할 수 없다**가 섞여 있지 않은지도 본다.
- **붙는 자리**: "이 지표로는 안 잡힌다"는 모든 진술, 대체 지표 제안.

### 2-4. 오차의 **종류**를 구분했나?
- **실패**: 무작위 오차는 표본으로 줄지만 계통 오차는 안 준다 — 관측을 **32배** 늘려도 편향은
  그대로였고, 그 몫이 13%→31%로 **커졌다**(#53). 편향을 겨냥한 대책은 잡음 한계를 못 넘는다.
- **덧**: 모든 정확도 숫자에는 **"어떤 센서로 잰 것인가"** 가 붙어 있다(같은 지표가 이상 센서
  0.81 → 현실 센서 0.61).
- **붙는 자리**: 반복 측정, 표본 수 늘리기, 정확도 스펙 인용.

### 2-5. 부품 지표가 아니라 **끝단 지표**로 채점했나?
- **실패**: 서보를 아무리 정밀하게 해도 앞단 정합이 틀리면 소용없다. 부품 지표는 전부
  좋아 보일 수 있다(#42).
- **덧**: 그리고 **지배항은 상황마다 뒤집힌다** — 개선할 곳을 직관으로 고르면 틀린다.
- **붙는 자리**: 파이프라인 전체 성능, 최적화 대상 선정.

### 2-6. **불확실도**를 결과의 일부로 냈나?
- 추정기는 값만이 아니라 **얼마나 못 믿는지**도 준다. 그걸 안전 마진으로 쓰면 위험한 동작을
  미리 멈출 수 있다(#9·#42).
- **붙는 자리**: 게이트/알람 설계, 자동 판정, 재작업 트리거.

### 2-7. 합격 기준이 **바닥인가 창인가?** ⚠️
- **실패**: 완주 기준을 "N 이상"으로 썼는데 표적은 그보다 위였다. 그러자 표적을 **지나치는**
  구성이 **가진 검사를 전부 통과**했다(#68 — 표적 55 mm, 도달 59.1 mm, 기준 45 mm).
  열여덟 실험이 **초과를 볼 수 없는 자**로 재고 있었다.
- **덧**: 기준은 성능을 재는 도구가 아니라 **무엇이 통과할지를 정하는 도구**다. 한쪽만 막으면
  최적화는 반대쪽으로 샌다 — 그리고 그건 부정행위가 아니라 **시킨 대로 한 것**이다.
  결과는 **기준 대비**가 아니라 **표적 대비**로 보고한다(같은 표에서 기준선은 4.2 mm 못 미쳤고
  새 구성은 4.1 mm 넘어섰는데, 기준은 **둘 다 통과**시켰다).
- **재위반**: 그 바닥은 **시스템이 넘을 수 있는 높이로 맞춰져 있었다**(#69). 표적이 55 인데
  기준이 45 였고, 열두 실험이 매번 통과하는 동안 **도구는 표적 앞 4.2 에 영원히 서 있었다.**
  창으로 다시 채점하니 통과가 **하나도 없다.** **기준을 시스템에 맞추면 통과만 남고 도착은
  사라진다** — 그리고 아무도 그걸 모른다. 기준의 출처를 물을 것: **표적에서 왔나, 시스템에서
  왔나?** (#69는 이미 선언돼 있던 허용 오차를 썼다 — 5-2 의 "트리거는 상위 기준에서".)
- **덧 2**: **지표가 최종값이면 경로를 못 본다.** 표적을 6 지나갔다가 돌아온 실행이 최종값으로는
  기준선과 같았다(#69). 초과가 해로운 양이면 **최댓값(또는 경로 전체)** 로 채점한다. 바닥 문제와
  **별개**이고, 둘이 겹치면 나쁜 설계가 모든 검사를 통과한다.
- **붙는 자리**: 수용 기준(acceptance criteria), SLA·SLO, 모델 평가 임계값, 보상 함수 설계,
  "N 이상이면 합격"이라고 적힌 모든 것. 그리고 p99 만 보는 모든 대시보드.

---

## 3. 시험을 설계할 때

### 3-1. 이 시험이 **실패할 수는 있나?** ⚠️
- **실패**: 가정을 걷어냈는데 네 절 동안 아무 일도 없었다. 채널이 튼튼해서가 아니라 **그 설정이
  과제를 완주하지 못해** 시험 대상을 여기하지 못한 것이었다(표적 55 mm 중 34.8 mm에서 멈춤).
  완주시키자 같은 조건이 **860배**의 에너지를 만들었다(#56).
- **재위반**: #62에서 같은 함정에 다시 빠졌다(깊이 34.7 mm). #64에서는 **검출 대상 하나만**
  흔들어서 AUROC가 1.00이 나왔다 — 지표가 **무해한 이유로도** 움직이게 만들어야 시험이 된다.
- **붙는 자리**: 회귀/스트레스 테스트, 벤치마크, 검출기 평가. **로버스트니스 결과는 완주
  지표와 함께 읽는다.**

### 3-2. 그 보증이 **덮는 범위**가 어디까지인가?
- **실패**: 아홉 실험이 한계 절에 "이 장부는 한 블록만 감싼다"고 적어만 두고 안 쟀다. 재보니
  **그 장부가 정확히 0인 동안 전체는 77~106 mJ를 만들고 있었다**(#65). 그리고 그걸 고쳤다는
  기법은 **자기가 재는 블록만** 고쳤다 — 위반 숫자는 지우고 실제 양은 그대로 뒀다.
- **재위반**: 그래서 **고치는 쪽**을 만들었더니 거기서도 같은 일이 났다(#66). 관대한 장부를 보게
  한 감시기는 그 장부만 1.1 mJ 로 내리고 안 보는 장부에 74 mJ 를 남겼다. **처방도 자기가 보는
  것만 고친다** — 그러니 무엇을 감시할지가 무엇이 보증될지를 정한다.
- **붙는 자리**: "검증 완료" 범위, 모듈 인증 vs 시스템, SLA·보증 문구.
- **증명이 덮는 영역 안에서의 만족은 전체에 대한 증거가 아니다.**

### 3-3. **독립적으로 두 번** 만들어 대조했나?
- **실패**: 6축 동역학을 두 방식으로 독립 구현해 1e-10까지 맞춘 뒤에야 믿었고, 그 교차검증이
  실제 버그를 잡았다(#45). 다른 실험은 해석해(`y = Fℓ³/3EI`)와 대조하다 **이중적분을 한 번만
  누적한 것**을 찾았다 — 처짐이 수백 배 작게 나오는데 **그림은 그럴듯했다**(#48).
  **둘 다 눈으로는 안 잡히는 종류였다.**
- **덧**: 중복은 **천장을 올리는 게 아니라 실패를 없앤다** — 다른 물리 채널의 중복 단서가
  첫 단서가 놓친 경우만 정확히 메웠고, 나머지는 비트 단위로 같았다(#62).
- **덧 2**: **대조가 대조인지도 확인한다.** #69에서 어떤 상수를 배율로 쓸었는데 다섯 배율이
  전부 같은 숫자였다 — 상위 모듈을 고쳤는데 실제로 쓰이는 건 import 시점에 복사된 값이었기
  때문이다. 그대로 읽었으면 **"그 값은 무관하다"** 는 정반대 결론이 나온다. **손잡이가 실제로
  먹는지부터 한 줄로 확인**하고, 안 움직이는 스윕은 결과가 아니라 **버그**로 의심한다.
- **붙는 자리**: 핵심 수식, 참조 구현, 이중 계산, 교차 검증.

### 3-4. **합성에서 된 것**을 실데이터에서 확인했나?
- **실패**: 안전 게이트가 합성에서 85%를 잡다가 실측에서 **5%**로 붕괴했다(#44). 실패의
  **성질**이 바뀌었기 때문이다("병조건" → "확신에 찬 오답").
- **붙는 자리**: 시뮬레이션 검증, 합성 데이터 학습, 파일럿 → 현장.

### 3-5. **주어진 것**을 하나씩 빼 봤나?
- **실패**: 사전지식 → 그 가정의 검증 → 그 검증의 센서 → 폐루프 이득 → 대응 → 상수 지연을
  차례로 뺐더니 매번 숫자가 바뀌었다(#51~#56).
- **재위반**: 아홉 실험이 같은 항을 범인으로 적었고 이득을 **쓸어서** 문제가 따라 자라는 것까지
  봤는데, 정작 **꺼 보니** 문제의 대부분이 남았다(#66 — 항을 완전히 꺼도 16 mJ). **쓸기는 상관,
  끄기는 인과다.** 항끼리 상쇄하는 계에서는 항별 분해조차 흐름일 뿐 원인을 못 정한다.
- **세 번째**: 그 남은 몫마저 결함이 아니었다(#67). 손잡이를 **둘 다** 움직여 격자로 보니
  **문제 없이 일을 마치는 칸이 아예 없다** — 비용과 성과의 순위상관이 0.92 다. 찾던 '불량 부품'은
  처음부터 없었고 **그건 가격표였다.** 절제는 한 손잡이의 범인성만 반증하고, **비용이 성과에
  붙어 있는지**는 2 차원에서만 보인다.
- **붙는 자리**: 절제 실험, 기여도 귀속, "이 개선이 무엇 덕인가".
- **결과를 늘리는 것보다 자기 결과의 유효 범위를 좁히는 쪽이 신뢰를 만든다.**

---

## 4. 결과를 읽을 때

### 4-1. **짝지어** 비교했나? ⚠️
- **실패**: 오른쪽으로 치우친 두 분포의 중앙값을 비교하면 짝지은 효과를 **과장한다**. 중앙값
  대 중앙값 1.8배가 짝지으면 감소율 25%·개선 11/16이었다(#59).
- **재위반**: #62에서 6시드 중앙값으로 보고 "이득 없음"이라 쓸 뻔했는데, 12시드 짝지은 통계에서
  **부호가 반대**로 나왔다.
- **재위반**: #66에서 짝짓지 않은 채로 "끌림이 39% 줄었다"를 쓸 뻔했다 — 같은 깊이로 맞추자
  차이가 사라졌다(10.1 vs 9.3 mm). **덜 해서 덜 상한 것**을 개선으로 읽는 함정이다.
- **덧**: 시끄러운 지표는 **시드 수를 늘리고 짝지은 차 + 개선한 시드 수**를 같이 낸다.
  "모든 시각·모든 실행"의 조건(수동성·안전 한계 등)이면 중앙값이 아니라 **최악 케이스**로 묻는다.
  그리고 최악 케이스는 **본 만큼만** 참이다 — #66에서 6시드까지 정확히 0이던 위반이 12시드에서
  둘 나왔다. 중앙값 함정이 **표본 수** 함정으로 다시 온다.
- **붙는 자리**: A/B 비교, 조건별 성능, 랜덤 시드가 있는 모든 실험.

### 4-2. 그 측정이 **결정을 가르기는 하나?**
- **실패**: "이제 그 값을 재러 가자"로 끝난 다음 실험에서 먼저 계산해 보니, 그럴듯한 범위
  어디서도 **결론이 안 바뀌었다.** 구속변수는 그 값이 아니라 **선언해야 하는 다른 숫자**였다(#60).
- **덧**: 재기로 했다면 둘을 더 본다 — 갖고 있는 로그에서 그 값이 **교락**되어 있지 않은지
  (상수항이 두 값의 합과 소수점까지 같으면 잡음이 아니라 **정보의 부재**다), 그리고 필요한
  여기가 참값에 달려 있으면 **사다리를 올려 값이 안 자라는 지점**을 찾는다(참값을 몰라도 수렴은 안다).
- **붙는 자리**: 추가 실험/계측 착수 결정, 데이터 수집 계획, 센서 스펙 선정.
- **측정 비용을 쓰기 전에 정보의 값을 계산하는 것이 순서다.**

---

## 5. 고칠 때

### 5-1. 없애려는 그 항이 **조용히 하고 있는 일**이 있나?
- **실패**: "증명이 덮지 않는 결함"이라 지목한 항을 함께 죄었더니 **더 나빠졌다**(4.72 → 6.53 mm).
  그 항이 통신 두절 동안 도구를 붙잡는 **브레이크**였기 때문이다(#57).
- **붙는 자리**: 레거시 코드 제거, 하드코딩 정리, "불필요해 보이는" 방어 로직.
- **"없애라"가 아니라 "그 항이 하는 일을 먼저 찾아 대체하라"** 가 옳은 순서다.

### 5-2. **발견 → 규칙 → 대체물 → 재시험**까지 갔나?
- **실패**: 한 실험은 "보증 밖 항이 브레이크였다"를 발견하고 규칙만 적었다. 다음 실험이 대체물을
  만들어 확인하고 나서야 원래 처방이 무해해졌다(#57 → #58).
- **덧**: **트리거를 어디서 가져오는지가 설계의 질을 정한다** — 임계값을 튜닝하면 임의의 숫자가
  되고, 이미 선언된 상위 기준에서 가져오면 상황마다 자동으로 맞는 값이 된다.
- **붙는 자리**: 인시던트 후속 조치, 포스트모템 액션, "다음엔 조심하자"로 끝나는 모든 것.
- 마지막 단계까지 안 가면 규칙은 **검증되지 않은 채로** 남는다.

### 5-3. 그 규칙을 **코드와 테스트**에 넣었나?
- **실패**: 자기가 만든 규칙 둘을 자기가 다시 어겼다(#62). 그래서 짝지은 비교를 **실험 자체의
  헬퍼**로 넣었다.
- **붙는 자리**: 코딩 컨벤션, 리뷰 체크리스트, 문서로만 존재하는 모든 합의.
- **기억에 의존하는 규칙은 두 번째에 또 어긴다.**

### 5-4. 손잡이를 **하나씩만** 돌리고 있지 않나?
- **실패**: 열한 실험 동안 이득을 **한 번에 하나씩만** 쓸었다. 그래서 두 가지가 같이 일어났다 —
  (1) 비용이 특정 부품의 **결함**으로 보였는데, 두 손잡이를 격자로 같이 움직이자 **성과를 내는
  칸은 예외 없이 비쌌다**(#67, 순위상관 0.92). 찾던 불량 부품은 처음부터 없었고 **그건
  가격표였다.** (2) **같은 성과에 37% 싼 자리**가 바로 옆에 있는 것도 못 봤다.
- **덧**: 1 차원 사다리는 **곡선**을 주고 격자는 **지형**을 준다. "이 축으로는 못 산다"는
  사다리로 말할 수 있어도 **"어디서도 못 산다"는 격자 없이 말할 수 없다.**
- **재위반**: 그런데 그 격자조차 **한 축이 빠져 있었다**(#68). 처음에 고르고 열여덟 번 동안
  아무도 안 건드린 값이 있었고, "어디서도 못 산다"는 **그 값을 고정한 절단면 위에서의 진술**
  이었다. 움직여 보니 칸이 다섯 개 있었다. **한 번 정하고 다시 안 본 값은 조용히 모든 결론의
  전제가 된다** — 그리고 그 사실은 흔들어 보기 전까지 안 보인다. 그러니 무엇을 스윕할지 고를 때
  **스윕 목록에 없는 상수부터 적어 보는 것**이 순서다. (그리고 격자 위에서 정말 못 사는 것이
  확인되면, 답은 손잡이가 아니라 **구조를 바꾸는 쪽**에 있다.)
- **붙는 자리**: 하이퍼파라미터 튜닝, 임계값·이득 선정, 용량 계획, "최적 설정" 주장.

---

## 마지막 하나

이 목록에서 가장 자주 값이 나간 건 특정 항목이 아니라 태도였다 —
**예측이 맞았는지보다 왜 틀렸는지가 자산이다.**

이 저장소에서 예측이 틀린 실험이 여덟 번 있는데, 그때마다 **왜 틀렸는지가 예측이 맞았을 때보다
값이 나갔다.** "결론이 좁아질 것"이라 적었는데 안 좁아졌고(#57), 그 이유가 브레이크 발견으로
이어져 정지 설계(#58)까지 갔다. "새 축을 열면 정책이 뒤집힐 것"이라 봤는데 안 뒤집혔고,
대신 **정보의 값이 뒤집혔다**(#63). "지목된 항을 죄면 보증이 온다"고 적었는데 안 왔고, 그
실패가 **지목 자체가 부분적으로만 옳았다**를 꺼내 줬다(#66).

그러니 예측은 **적어 두고** 시작한다. 안 적으면 틀린 것도 모른다.

---

**출처**: 이 항목들은 [LEARNING_PATH.md](LEARNING_PATH.md)의 반복 교훈과
[VERIFICATION.md](VERIFICATION.md)의 요구사항 R1–R34 · 위험 H1–H44에서 뽑았다.
각 항목의 원래 맥락과 숫자는 [README.md](README.md)의 실험별 절에 있다.
