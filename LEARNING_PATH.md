# 로봇을 처음 배우는 사람을 위한 학습 경로

이 저장소는 실험 61개가 번호 순서로 쌓여 있어서, 로봇을 처음 보는 사람이 열면 어디서부터
봐야 할지 알기 어렵다. 이 문서는 **"로봇을 공부한다"는 관점으로 그것들을 다시 배열한
안내서**다(실험이 늘어나도 8단계 배열은 그대로 두고 해당 단계에 끼워 넣는다). 각 단계마다
(1) 그 단계가 답하려는 질문, (2) 알아야 할 개념, (3) 돌려볼 스크립트,
(4) 결과에서 무엇을 봐야 하는지, (5) 처음 배우는 사람이 흔히 오해하는 지점을 적었다.

수식을 외울 필요는 없다. 각 실험이 그림 한 장과 숫자 몇 개를 내놓으므로, **"이 숫자가 왜
이렇게 나왔지?"** 를 따라가면 개념이 붙는다.

---

## 0. 준비

```bash
pip install numpy matplotlib pytest scipy      # 전부 이것만 필요 (torch는 실험 16만, 선택)
python scripts/01_tracking.py                  # 첫 실험
pytest -q                                      # 전체 검증 (약 6~15분, 310개 테스트)
```

실행하면 `outputs/` 에 그림이 저장되고, 터미널에 숫자 요약이 찍힌다. 각 스크립트 맨 위
독스트링에 "무엇을 왜 하는지"가 한국어로 적혀 있으니, **코드를 읽기 전에 독스트링부터**
읽는 편이 빠르다.

---

## 로봇이 푸는 문제는 결국 네 가지다

로봇 소프트웨어를 처음 보면 용어(SLAM, EKF, MPC, ICP…)가 쏟아지는데, 하는 일은 네 갈래다.

| 질문 | 분야 이름 | 이 저장소의 단계 |
|---|---|---|
| **나는 어디 있나?** (센서는 잡음·끊김이 있는데) | 상태추정 / 측위 | 1~3단계 |
| **주변은 어떻게 생겼나?** (지도가 없는데) | SLAM / 매핑 | 4~5단계 |
| **어디로 어떻게 갈까?** | 계획 / 제어 | 6단계 |
| **모델이 틀렸을 때 어떻게 하나?** | 학습·sim-to-real·시스템식별 | 7단계 |

그리고 이 저장소에는 축이 하나 더 있다: **의료 로봇(영상유도 시술)** — 위 네 가지를 하나의
사슬로 이어서 "환자에게 전달되는 오차 하나"로 채점하는 8단계다.

---

## 1단계 — 잡음 속에서 위치를 알아내기 (칼만 필터)

**질문**: 센서 값이 흔들리는데, 어떻게 매끄럽고 정확한 위치를 얻나?

**개념**: 칼만 필터는 두 가지를 저울질한다 — ① 물리 모델의 예측("등속으로 갔으면 여기쯤"),
② 방금 들어온 측정값. 어느 쪽을 더 믿을지는 각각의 **불확실도(공분산)** 가 정한다. 신호처리를
아는 사람에게는 "잡음비에 따라 대역폭이 자동으로 조절되는 필터"로 읽힌다.

**돌려볼 것**
```bash
python scripts/01_tracking.py     # 곡선으로 움직이는 표적을 잡음 측정에서 복원
python scripts/02_imu_fusion.py   # 위치 센서 + IMU 융합, 그리고 센서가 끊겼을 때
```

**볼 것**: 01에서 "생 측정값 → 이동평균 → 칼만"의 오차가 줄어드는 표. 02에서 센서가 끊긴
구간(음영)에서도 추정이 이어진다 — IMU로 **관성주행**하기 때문이다. 융합 오차가 어떤 단일
센서보다 작다는 것이 핵심.

**흔한 오해**: "필터는 그냥 부드럽게 만드는 것" → 아니다. 부드럽게만 하면 빠른 움직임에서
뒤처진다. 칼만은 *언제 믿고 언제 무시할지*를 계산한다.

---

## 2단계 — 세상은 직선이 아니다 (비선형 필터)

**질문**: 로봇이 회전하면 운동 모델이 비선형이 된다. 그러면 칼만을 어떻게 쓰나?

**개념**: 두 가지 방법이 있다. **EKF**는 비선형 함수를 매 순간 접선으로 근사(야코비안)하고,
**UKF**는 대표점(시그마 포인트)을 통과시켜 통계를 얻는다. 그리고 센서에는 보통 **바이어스**
(살짝 치우친 값)가 있는데, 이것도 상태에 끼워 넣어 같이 추정할 수 있다.

**돌려볼 것**
```bash
python scripts/03_ctrv_ekf_ukf.py   # 선회하는 표적: EKF vs UKF
python scripts/04_imu_bias.py       # IMU 바이어스를 온라인으로 추정
```

**볼 것**: 비선형 모델이 선회 구간에서 22% 좋아지지만 **EKF와 UKF는 사실상 비슷**하다(정직한
결과). 04에서는 측정이 들어올 때만 바이어스 추정이 움직이고, 측정이 없으면 멈춘다.

**여기서 처음 만나는 큰 개념 — 관측성(observability)**: 데이터가 그 값에 대해 아무 정보도
주지 않으면, 추정기는 그것을 알아낼 수 없다. 이 저장소에서 계속 반복되는 주제다(4·37·43·46).

---

## 3단계 — 서로 다른 센서를 엮기 (VIO)

**질문**: 카메라는 방향만 알려주고(거리 모름), IMU는 흔들림을 잘 잡지만 오래 못 간다.
합칠 수 있나?

**개념**: 상보적인 센서를 하나의 상태 추정에 넣는 것이 **센서 융합**의 본질이다. 시각-관성
오도메트리(VIO)는 IMU의 짧은 시간 정확도와 카메라의 장기 안정성을 맞바꾼다.

**돌려볼 것**: `python scripts/08_vio.py` (드리프트 3배 감소)

**흔한 오해**: "센서를 많이 붙이면 좋아진다" → 정보가 겹치기만 하면 별로 안 좋아진다.
**서로 다른 축을 관측하는** 센서가 붙어야 이득이 크다.

---

## 4단계 — 지도가 없을 때 (SLAM)

**질문**: 지도가 없는데 내 위치를 알아야 하고, 내 위치를 모르는데 지도를 만들어야 한다.
닭이 먼저인가 달걀이 먼저인가?

**개념**: **SLAM**(동시적 위치추정 및 지도작성)은 둘을 같이 푼다. 두 가지 계열이 있다.
- **필터 계열(EKF-SLAM)**: 로봇 위치와 랜드마크 위치를 한 상태 벡터에 넣고 계속 갱신
- **그래프 계열(pose-graph)**: 위치들을 노드, 관측을 엣지로 두고 **전체를 한꺼번에 최적화**.
  같은 장소를 다시 방문했다는 사실(**루프 클로저**)을 엣지 하나로 넣으면, 궤적 전체가
  뒤로 소급해서 교정된다 — 처음 보면 마법처럼 보이는 부분이다.

**돌려볼 것**
```bash
python scripts/05_ekf_slam.py          # 필터 계열
python scripts/06_loop_closure.py      # 루프 클로저의 효과만 따로
python scripts/07_pose_graph_slam.py   # 그래프 계열(Gauss-Newton 최적화)
```
👉 **브라우저 데모**: [pose-graph SLAM](https://yeonkyunlee.github.io/sensor-fusion-lab/slam_demo.html)
— 로봇을 직접 몰아 드리프트를 쌓고, 루프가 닫히는 순간을 눈으로 본다. 이 단계는 글보다 데모가 빠르다.

**볼 것**: 05의 디버깅 기록(실험 독스트링)에 **8번 실패한 과정**이 남아 있다. 각도의 ±π
경계 처리, 헤딩 관측성 같은 것들이 초보자가 반드시 밟는 함정이다.

**흔한 오해**: "SLAM은 라이브러리 부르면 되는 것" → 실제로 시간을 잡아먹는 건 알고리즘이
아니라 **좌표계·각도 랩어라운드·잘못된 데이터연관**이다.

---

## 5단계 — 실제 센서, 실제 데이터, 그리고 거짓 정보

**질문**: 합성 데모는 잘 되는데 진짜 데이터에서도 되나? 관측이 **틀렸을** 때는?

**개념**
- **ICP**: 두 점군(라이다 스캔)을 겹쳐서 상대 이동량을 알아내는 방법. 라이다 SLAM의 입구.
- **강건 최적화**: 루프 클로저가 잘못 잡히면(같은 장소라고 착각) 지도가 찢어진다. Huber·DCS
  같은 **강건 커널**이 이상치의 영향력을 깎는다.
- **표준 벤치마크**: 내 구현이 옳은지 확인하려면 **남들이 쓰는 실제 데이터셋**에서 돌려봐야 한다.

**돌려볼 것**
```bash
python scripts/21_icp_scan_matching.py   # ICP 오도메트리 (9.3배 개선)
python scripts/23_lidar_slam.py          # 2D 라이다 SLAM 전체 (ICP + 포즈그래프)
python scripts/11_robust_slam.py         # 거짓 루프클로저 거부
python scripts/14_g2o_benchmark.py --file data_cache/intel.g2o   # 실제 벤치마크
```

**볼 것**: 14번은 SLAM 논문들이 공통으로 쓰는 데이터셋(Intel, parking-garage)에서 χ²(오차
합)이 5,150,000 → 216 으로 떨어진다. **"합성에서 되는 것"과 "실데이터에서 되는 것"은 다르다**는
사실을 처음 확인하는 자리다.

---

## 6단계 — 이제 움직여 보자 (계획과 제어)

**질문**: 위치를 알았다. 그래서 어떻게 목표까지 가나?

**개념 세 층**
1. **전역 계획**: 지도에서 목표까지의 경로 찾기 (A\* 탐색)
2. **지역 제어**: 그 경로를 실제로 따라가기 (pure-pursuit — 앞의 한 점을 보고 조향)
3. **예측 제어(MPC)**: 앞으로 몇 초를 미리 시뮬레이션해서 **가장 좋은 조향을 지금 고르기**.
   액추에이터 한계(최대 속도·회전)를 제약으로 넣을 수 있다는 게 큰 장점.

**돌려볼 것**
```bash
python scripts/19_plan_control.py     # A* + pure-pursuit: 추정→계획→제어 폐루프
python scripts/20_dwa_dynamic.py      # 움직이는 장애물 회피
python scripts/24_mpc_tracking.py     # MPC (pure-pursuit보다 3배 정밀)
python scripts/28_full_navigation.py  # 전역계획 + 지역제어 통합 캡스톤
```

**볼 것**: 28번에서 **전역 계획이 모르는 이동 장애물**을 지역 제어가 실시간으로 피해 간다.
같은 A\* 경로를 장애물 무지 추종기로 따라가면 최소 여유거리가 음수(= 충돌)가 되고, 장애물
인지 MPC를 붙이면 +0.62 m를 유지한다 — 동적인 환경에서는 지역 반응성이 안전을 만든다.

---

## 7단계 — 모델이 틀렸을 때 (학습, sim-to-real, 시스템식별)

**질문**: 지금까지는 물리 모델을 안다고 가정했다. 모르거나 틀렸으면?

**개념**
- **시뮬레이터에서 학습**하고 실제로 옮기면 성능이 무너진다(**sim-to-real gap**).
- **도메인 랜덤화**: 시뮬에서 물리 파라미터를 매번 흔들어 학습 → 최고 성능을 조금 내주고
  **넓은 강건성**을 산다.
- **시스템 식별**: 실제 기기에서 로그를 모아 **모델 파라미터를 데이터에 맞추는** 것. 로봇 팔
  동역학은 관성 파라미터에 대해 **선형**이라, 회귀 한 번으로 질량·마찰까지 복원된다.
- **리워드 설계**: 강화학습의 어려움은 대개 옵티마이저가 아니라 **목표 함수**다.

**돌려볼 것**
```bash
python scripts/30_domain_randomization.py  # 강건성을 사는 대가
python scripts/31_simopt_loop.py           # 실기 로그로 시뮬을 보정하는 폐루프
python scripts/32_reward_shaping.py        # 리워드 해킹
python scripts/38_model_based_rl.py        # 모델을 배워서 계획하기 (데이터 효율 ~473배)
```

**볼 것**: 32번에서 "에너지를 키워라"라고 리워드를 잘못 주면, 정책이 진자를 영원히 돌려
**리워드는 높은데 실제 성공률은 0.00** 이 된다. 지표를 잘못 고르면 최적화가 그 지표를
"해킹"한다는, 로봇을 넘어 모든 자동화에 통하는 교훈이다.

---

## 8단계 — 모든 걸 하나의 사슬로: 의료 로봇 (영상유도 시술)

여기가 이 저장소의 종착지다. 앞의 모든 조각이 **직렬로** 연결되어, 마지막에 숫자 하나로
채점된다: **도구 팁이 계획한 표적에서 몇 mm 벗어났는가.**

```
[수술 전] CT 영상에서 표적·피해야 할 혈관·진입 경로 계획
   ↓  환자가 미지의 자세로 수술대에 놓인다
[정합] 표면을 프로브로 찍어 "영상 좌표 ↔ 로봇 좌표" 변환을 복원 (ICP)
   ↓
[계획] 영상에서의 계획을 로봇 좌표로 옮기고, 통로 여유를 확인
   ↓
[기구학] 팔의 관절 각도를 계산 (역기구학 IK)
   ↓
[제어] 팔의 동역학을 알고 토크를 계산해 궤적을 추종
   ↓
[결과] 실제 팁 위치 vs 진짜 표적 = 엔드투엔드 표적 오차
```

**새로 배우는 용어 네 개**
- **순기구학/역기구학(FK/IK)**: 관절 각도 → 손끝 위치가 FK, 그 반대가 IK.
- **야코비안**: "관절을 이만큼 돌리면 손끝이 이만큼 움직인다"는 비례 관계 행렬. IK·특이점·
  힘 변환이 전부 여기서 나온다.
- **특이점(singularity)**: 팔이 특정 자세(예: 완전히 편 상태)에서 어떤 방향으로 움직일 능력을
  잃는 것. 이때 순진한 IK는 **관절 속도를 폭발**시킨다.
- **정합(registration) / TRE·FRE**: 정합은 영상과 환자를 같은 좌표계로 묶는 일. **FRE**는
  정합에 쓴 표면점의 잔차(측정 가능), **TRE**는 실제 표적에서의 오차(환자가 겪는 것).
  **FRE가 낮아도 TRE는 클 수 있다** — 이 저장소가 실측으로 보여주는 임상 함정.

**돌려볼 것 (순서대로)**
```bash
python scripts/39_manipulator_kinematics.py  # 팔의 기구학: FK·야코비안·IK·특이점
python scripts/40_manipulator_dynamics.py    # 팔의 동역학: 모델을 알면 왜 정확한가
python scripts/41_surgical_registration.py   # 환자-영상 정합 (ICP), FRE vs TRE
python scripts/42_image_guided_targeting.py  # 위 셋을 사슬로 + 오차 예산 + 안전 게이트
python scripts/45_image_guided_6dof.py       # 평면을 벗어나 실제 6축 팔로
python scripts/47_needle_impedance.py        # 조직에 닿으면? 임피던스 제어
python scripts/48_flexible_needle.py         # 바늘이 휘면? 스핀으로 보상
python scripts/49_registration_real_anatomy.py  # 실제 사람 MR 스캔에서 정합
python scripts/50_teleoperation_delay.py     # 원격조작: 지연·수동성·가상 고정구
python scripts/51_deformable_registration.py # 조직이 눌리면? 강체 가정이 깨지는 지점
python scripts/52_probing_the_prior.py       # 그 가정이 맞는지 어떻게 아는가
python scripts/53_measurement_changes_it.py  # 재는 행위가 대상을 바꾸면?
python scripts/54_closed_loop_needle.py      # 휨 보상을 폐루프로: 추정 vs 제어 권한
python scripts/55_correspondence_search.py   # 대응을 스스로 찾으면? 표면이 못 보는 방향
```

**50번은 "사람이 루프에 있는" 첫 실험**이다. 여기까지는 계획을 주면 로봇이 알아서 실행했지만,
실제 수술 로봇은 술자가 마스터를 잡고 조작하고 조직의 힘이 손으로 되돌아온다. 배우는 것:
- **힘을 되돌려주면 불안정해질 수 있다.** 지연된 채널은 에너지를 만들어낼 수 있어서, 직접 힘반사는
  50 ms부터 손끝이 떨린다. 파동변수로 바꾸면 200 ms에서도 버틴다(대신 손이 무거워지고 위치가 표류).
- **힘 피드백을 잘못 하면 없는 것보다 나쁘다.** P-P는 조직이 아니라 결합 스프링을 느끼게 해서
  힘 오차가 4.01 N(실제 조직력 1.3 N)이다.
- **안전장치도 지연에 묶인다.** 금지구역 벽을 마스터에서 로컬로 계산하면 아주 딱딱하게 만들 수
  있지만(침범 168배 감소), 지연 경로로 계산하면 800 N/m부터 채터링하고 그 이상에서 발산한다.

👉 **브라우저 데모**: [영상유도 정합 데모](https://yeonkyunlee.github.io/sensor-fusion-lab/guided_demo.html)
— 환자 표면을 직접 드래그해서 찍어 보면, **어디를 찍느냐**가 심부 표적 오차를 정하는 걸 손으로 느낀다.
매끄러운 곳만 찍으면 해가 미끄러지고, 특징적인 곳을 포함하면 잡힌다. 이 단계는 글보다 데모가 빠르다.

**49번은 이 사슬에서 가장 "임상에 가까운" 실험**이라 따로 볼 만하다. 공개된 실제 인체 MR로
머리 표면을 만들고, 신경외과 내비게이션의 실제 절차(랜드마크 조대정렬 → 표면 정밀화)를 따른다.
여기서 배우는 세 가지가 초보자에게 특히 유용하다.
- **조대정렬 없이는 ICP가 아예 안 된다**: 표면의 6% 조각과 머리 전체의 무게중심을 맞추면 TRE
  92.94 mm, 랜드마크 4점을 쓰면 0.68 mm (136배). "ICP를 돌렸는데 안 된다"의 대부분은 초기값 문제다.
- **어디를 찍느냐가 정확도를 정한다**: 매끄러운 두피 영역 1.26 mm vs 코·눈두덩처럼 특징적인
  영역 0.49 mm. 구면에 가까운 조각은 표면을 따라 미끄러져 회전이 구속되지 않는다(= 관측성).
- **화려한 지표보다 검산 절차가 강하다**: 정합에 쓰지 않은 점 하나를 더 찍어 확인하는 임상 절차가
  실패를 100% 잡는데, 공분산 기반 게이트는 44%밖에 못 잡는다.

**51번은 이 사슬의 마지막 가정을 깬다.** 41·44·45·49번의 정합은 전부 "환자는 강체다"라고
가정했다. 조직은 안 그렇다 — 개두를 하면 뇌가 중력과 뇌척수액 유출로 내려앉는다(brain shift,
문헌상 수 mm~2 cm). 표적 위치에서 생긴 그 어긋남은 **어떤 강체변환으로도 지울 수 없다.**
초보자가 여기서 얻을 것은 정합 기법이 아니라 **정규화(regularization)를 보는 눈**이다.
- **표면은 보이는데 표적은 깊다.** 노출된 표면에서 잰 변위로 깊은 곳을 맞추는 건 보간이 아니라
  **외삽**이고, 외삽의 품질은 데이터가 아니라 **가정**이 정한다. 표면의 4%만 보이는 상황에서
  자유형 워프는 3.31 mm, 같은 보간기에 "창 밖 두피는 두개골이 잡고 있으니 안 움직인다"는
  사실 하나만 넣으면 0.60 mm다. 이긴 건 알고리즘이 아니라 가정이다.
- **그 가정은 공짜가 아니다.** 표면의 62%가 보이면 그 이득이 사라진다(1.43 vs 1.44 mm) —
  데이터가 충분하면 사전지식은 할 일이 없다. 반대로 가정이 틀린 환자에게는 같은 정규화가
  **편향의 원인**이 된다. 정규화는 개선이 아니라 **가정을 사는 것**이다.
- **"물리 기반"이라는 말에 속지 않기.** 부피 전체에 ∇²u = 0 을 푸는 모델을 넣고 격자를
  촘촘히 해봤더니 결과가 **나빠졌다**(1.12 → 2.38 mm). 이산화 오차가 아니라 모델 편향이다 —
  틀린 방정식을 더 정확히 풀었을 뿐이고, 성긴 격자가 나았던 건 경계가 한 칸 안쪽에 놓여
  오차가 우연히 상쇄됐기 때문이다. **수렴 검사는 푸는 사람을 검증하지, 방정식을 검증하지 않는다.**
- **FRE≠TRE가 더 사납게 돌아온다.** 표면 잔차가 가장 작은 방법이 심부에서는 가장 나쁘다.
  볼 수 있는 지표로 방법을 고르면 틀린 걸 고른다.

**52번은 51번이 남긴 질문에 답한다 — "그 가정이 맞는지 어떻게 아는가?"**
51번에서 정확도를 벌어준 사전지식은 정작 그것이 적용되는 데이터로 검증되지 않는다. 그래서 52번은
**표면에서 원리적으로 보이지 않는** 변형 모드를 하나 만들어 넣는다(깊이 45 mm 근처의 횡방향 이동:
창 안 표면에 남기는 자취 평균 0.03 mm — 프로브 잡음 1.0 mm 아래다. 그런데 표적에서는 2~6 mm).
초보자에게 이 실험이 주는 것은 정합 기법이 아니라 **관측성(observability)이라는 렌즈**다.
- **알고리즘 문제가 아니라 관측 문제다.** 심부 관측이 0개면 사전지식이 있든 없든 똑같이 3.77 mm에
  갇힌다. 표면 데이터 안에 그 정보가 **없기 때문**이다. 4·37번의 IMU 바이어스, 43·46번의 스틱션과
  정확히 같은 얼굴 — 이 문서의 첫 번째 교훈("관측되지 않는 것은 추정되지 않는다")이 여기서 다시 나온다.
- **지금 가진 지표는 정확히 쓸모없다.** 두 환자의 표면 잔차가 1.74 vs 1.74 mm로 같다. 그걸로
  게이트를 만들면 판별력 AUROC **0.52 = 동전 던지기**다. 환자 300명 중 심부 모드 환자의 88%가
  허용치를 넘는데 하나도 못 잡는다.
- **심부 관측 하나가 그걸 0.81로 바꾼다**(2개 0.85, 3개 0.90). 표적 근처에 두면 아무 데나 두는
  것(0.71)보다 낫다 — 49번의 "어디를 찍는가"가 심부에서 반복된다.
- **첫 관측 몇 개는 고치는 것보다 아는 것을 산다.** 같은 관측 1개를 교정에 쓰면 오차가
  3.10 → 2.49 mm로만 내려간다(여전히 허용치 밖). 검산에 쓰면 판별력이 0.52 → 0.81이 된다.
- **정직한 네거티브 하나.** 게이트를 쓰는 정책이 "그냥 관측 4개 다 받기"를 이기지 못했다.
  관측이 싸면 게이트를 만들지 말고 그냥 재라. 게이트의 값은 관측이 비쌀 때, 그리고 **고칠 수 없을 때
  경고를 낼 수 있다**는 데 있다.

**53번은 그 관측을 정직하게 만든다 — 그리고 52번의 숫자를 깎는다.** 52번의 초음파는 이상적이었다
(정답 변위 + 등방 잡음). 실제 초음파는 세 가지가 다르고, 각각이 서로 **다른 것**을 망가뜨린다.
초보자가 여기서 배우는 것은 정합이 아니라 **오차의 종류를 구분하는 법**이다.
- **계통 오차(bias)와 무작위 오차(noise)는 다르게 행동한다.** 프로브로 조직을 누르면 관측이
  안쪽으로 치우치는데, 그 방향이 **늘 같아서** 여러 번 재도 상쇄되지 않는다. 이상 센서 곡선과
  압박 곡선이 나란히 가고 격차가 +0.5 mm로 고정된다. 관측을 32배로 늘리면 잡음은 √N으로 줄어드니
  **압박의 몫이 13% → 31%로 커진다** — 데이터를 모을수록 계통 오차가 지배항이 된다.
  (이게 "무작정 데이터를 더 모으면 좋아진다"가 틀리는 대표적인 경우다.)
- **가중치는 정보를 만들지 못한다.** 깊을수록 σ가 커지는 걸 알고 λ에 반영하면 12% 회복된다.
  올바른 처리지만, 없는 정보를 만들어내지는 않는다.
- **대책은 자기가 겨냥한 오차원만 고친다.** 압박 보정·로버스트(편향·이상치용)는 교정 오차를
  2.84 → 1.82 mm로 살리는데, **게이트는 0.6에서 꿈쩍도 안 한다.** 게이트는 편향이 아니라 잡음에
  묶여 있기 때문이다(표적 깊이에서 신호 2~6 mm < 검산 1점 잡음 3.6~6.2 mm).
- 그래서 52번이 보고한 AUROC 0.81은 **검산 관측까지 이상적이라고 가정한 값**이었다. 검산도 같은
  센서로 하면 0.61이고, 검산점을 5개로 늘려도 못 되돌린다. **어떤 숫자든 "어떤 센서로 잰
  숫자인가"를 같이 물어야 한다.**

한 가지 더, 로버스트 추정을 처음 쓰는 사람이 반드시 밟는 함정도 여기 다 나온다 — TPS가 이상치를
그냥 보간해버려서 IRLS가 아무것도 못 보고(→ λ를 크게 시작해 푸는 어닐링), Huber의 부드러운 꼬리는
20 mm짜리 오대응을 못 걷어내고(→ 임계 밖을 0으로 보내는 Tukey), MAD로 스케일을 뽑으니 다수인
표면점이 스케일을 지배해 **소수의 유익한 심부 관측을 이상치로 오인해 삭제했다**(→ 알려진 σ 기준 +
적용 범위 제한). 셋 다 겪고 나서야 작동했고, 그 과정을 스크립트 독스트링에 남겨뒀다.

**54번은 48번의 열린 루프를 닫으며, 절제(ablation)가 왜 필요한지를 보여준다.** 48번의 스핀
flip은 곡률을 미리 안다고 가정했다. 조직 층이 바뀌면 최적 시점이 조직에 의존하게 되고
(r=0.4에서 17%, r=2.5에서 38%), 공칭 29.3%로 가면 최대 1.15 mm를 놓친다. 그래서 팁을 추적해
곡률을 온라인 추정하고 다시 계획했다 — 그리고 **결과를 정직하게 뜯어보니 예상과 달랐다.**
- **폐루프가 좋아 보였다**: p90 0.98 → 0.50 mm. 그런데 **측정을 한 번도 안 하는** 정책
  (모집단 평균값으로 계획)이 0.49 mm로 **똑같았다.** 개선의 실체는 정보가 아니라 ① 더 맞는
  기본값 ② flip을 늦추는 타이밍 제약이었다. 절제 없이 봤으면 "피드백 덕분"이라고 썼을 것이다.
- **왜 정보가 안 왔나**: 팁 *위치*는 곡률의 이중적분이라 정보가 (S−s_b)²/2로 늦게 온다.
  결정해야 할 20.5 mm에서 σ(κ̂₂)≈35/m, 사전분포 표준편차 0.49/m — 우도가 사전분포를 못 이긴다.
  5-DOF 센서로 *방향*까지 받으면(1차 적분이라 선형 증가) 0.47 mm — 간극의 6%만 메운다.
- **진짜 병목은 작동이었다**: 48번의 duty cycling으로 바꾸면 p90 0.37 mm. 그런데 **재계획을
  1회만 하면 flip과 정확히 같다**(명령이 포화해 u=−1이 되면 duty가 곧 flip이다). 이득은
  "다르게 조작"이 아니라 **"다시 조작"** 에서 나온다 — 정보가 늦었던 게 아니라 쓸 기회가
  한 번뿐이었다.
- **그리고 병목이 또 옮겨 간다**: 참 곡률을 줘도 0.36 mm, 센서를 10배 조용하게 해도 0.37 mm.
  이제 남은 건 재계획 간격·포화·모델이다. 42번의 "지배항은 뒤집힌다"가 반대편에서 다시 나온다.

**55번은 51~54가 내내 공짜로 받고 있던 것을 뺀다 — 대응(correspondence).** 프로브로 찍은 점이
모델의 *어느* 점이었는지, 지금까지는 알고 있었다. 실제로는 찾아야 하고, 보통 최근접점으로 찾는다.
- **접선 방향은 원리적으로 안 보인다.** 변형을 법선(표면이 안팎으로 움직임)과 접선(표면이 자기를
  따라 미끄러짐)으로 쪼개면, 매끄러운 표면을 접선으로 밀어도 **모양이 안 바뀐다.** 그래서 최근접점은
  그 몫을 못 보고, 잔차조차 남지 않는다. 접선을 0 → 8 mm로 키우면 대응 오차는 0.48 → 1.53 mm로
  늘지만 표면 잔차는 0.92 → 1.17 mm로 거의 그대로다. 광학 흐름의 **구멍 문제**와 같은 구조다.
- **대응을 찾는 비용은 2.6배다**: 정답 대응 0.54 mm → 최근접점 1.41 mm. 51~54의 모든 변형 숫자에
  이 몫이 빠져 있었다는 뜻이다.
- **표준 처방 셋이 다 실패한다.** 점-대-평면은 오히려 나쁘고(1.75 mm) 표면 잔차만 낮춘다(지표가
  또 배신한다). 랜드마크 2~16개는 무효 — 편향이 창 전체에 깔려 있어 앵커 몇 개로 못 덮는다.
  로버스트도 거의 무력한데, 이유가 재미있다: 관측점이 튀면 최근접점이 **그 근처의 다른 표면점**을
  찾아 주므로 대응은 틀렸는데 변위는 작고 그럴듯하다 — 잔차로 볼 것이 없다.
  **이상치는 '틀린 데이터'이고 접선은 '없는 데이터'다. 다른 병이라 다른 약이 필요하다.**
- 그래서 질문이 바뀐다: **대응 중 몇 %가 기하가 아닌 근거**(혈관 패턴 스테레오비전, 삽입 마커)로
  잡혀야 하나. 0% 1.41 → 25% 1.13 → 50% 0.74 → 100% 0.54 mm — **거의 선형이고, 싼 해법이 없다.**

**56번은 50번의 "지연은 상수다"를 뺀다. 그리고 여기서 배우는 것은 채널이 아니라 시험 설계다.**
- **먼저 아무 일도 안 일어났다.** 50번 설정 그대로 ±40 ms 지터와 40% 손실을 넣어도 채널은 수동이고
  (수동성 위반 ~10⁻⁵ mJ) 진동은 오히려 줄었다. 송신률을 1 kHz → 50 Hz로 떨어뜨려도 차이가 없다.
  **1 kHz는 국소 제어 루프가 요구하는 속도이고, 채널이 날라야 할 정보량이 아니다.**
- **그런데 그 설정은 과제를 완주하지 못한다.** 조직 반력 아래 정상상태 오차가 남아 도구가
  표적 55 mm가 아니라 34.8 mm에서 멈춘다. 채널이 거의 여기되지 않은 채로 시험을 통과한 것이다.
  **실패할 수 없는 시험은 시험이 아니다** — 공부할 때 가장 데이기 쉬운 지점이다.
- **완주시키자 같은 지터가 860배의 에너지를 만든다.** 표류 보정 이득을 올려 도구를 50.8 mm까지
  보내면 위반이 0.006 → 4.87 mJ가 된다. 결함은 내내 있었고 크기가 신호에 비례할 뿐이었다.
- **그 자리는 구조적으로 예견돼 있었다.** 표류 보정 λ(x_m − x_s) 항은 **파동 변환 밖**이라
  수동성 증명이 애초에 덮은 적이 없다. **증명은 내내 참이었고, 일을 하는 부분을 덮고 있지 않았다.**
- **표준 처방이 성능을 판다.** 디지터 버퍼로 지터를 추가 상수 지연으로 바꾸면 수동성은 돌아오지만,
  그 지연이 때리는 곳이 바로 보증 밖의 그 위치 루프라 진동이 0.21 → 1.60 mm로 커진다.
  대신 **에너지 예산**(보내온 에너지가 허락하는 만큼만 꺼내 쓰기)은 지연 없이 수동성을 되찾고
  진동도 가장 낮다(0.15 mm, 감쇠기 가동 5.7%). **버퍼는 상시로 내고 예산은 사건이 있을 때만 낸다.**
- **그리고 손실 내성은 알고리즘이 아니라 보내는 양의 성질에서 온다.** 예산을 **누적값**으로 보내면
  단조량이라 다음 패킷 하나가 손실 이력 전체를 복구한다(감쇠 가동 5%). 증분으로 보내면 잃은 몫이
  영영 사라져 감쇠기가 상시 켜진다(95%, 깊이 50.8 → 36.3 mm). 같은 알고리즘, 다른 페이로드.

**57번은 56번이 기대고 있던 대칭성을 뺀다 — 그리고 내 예측이 틀린다.** 56번은 "지연이 늘면 굶어서
에너지를 만들고 줄면 낡은 패킷을 버려서 없애므로, 평균 0 지터에서는 상쇄된다"로 자기 null 결과를
설명하고 한계에 "이 논리는 대칭성에 기댄다"고 적었다. 그래서 연집 손실(Gilbert-Elliott)과 긴 지연
꼬리(파레토 α=1.8, 분산 무한·최댓값 없음)를 넣었다. **평균 지연과 평균 손실률을 고정하고 모양만**
바꾼다 — 안 그러면 '연집성의 효과'가 아니라 '늘어난 지연의 효과'를 재게 된다.
- **예측이 틀렸다. 56번 결론은 대체로 버틴다.** 그런데 버티는 이유가 채널이 아니라 **플랜트**다:
  **낡은 명령을 붙들고 있는 것은 자기 제한적**이다. 낡은 명령에도 정지 평형이 있어서 팔이 거기로
  수렴해 멈춘다(60 ms 넘게 붙들면 초반의 1/3.3 속도). 그래서 평균 손실률을 고정한 채 연집을 80배로
  늘려도 총 맹행 거리는 오히려 **줄고**(17.5 → 6.6 mm) 한 사건의 크기가 포화한다.
- **그 브레이크를 만드는 항이, 56번이 '증명 밖의 결함'이라고 지목한 그 항이다.** 예산은 파동만 죄고,
  팔을 실제로 붙잡는 건 표류 보정 λ(x_m − x_s) — 낡았지만 **유계인 setpoint 를 향한 위치 servo**다.
  그래서 "λ도 같은 예산으로 죄자"는 당연한 처방이 오히려 맹행을 늘린다(4.72 → 6.53 mm).
  **56번의 진단은 절반만 맞았다: 같은 항이 보증을 깨면서 동시에 안전 기능을 하고 있었다.**
- **정작 무너진 것은 버퍼 설계다.** 파레토 꼬리에는 최댓값이 없으니 56번의 "45 ms 사면 끝"이 성립하지
  않고, 재생 기한을 두는 순간 **없던 손실이 만들어진다**(p50 버퍼에서 41% — 망 손실은 5%인데).
  작게 잡으면 모든 축에서 지고, 크게 잡으면 편도 328 ms를 상시로 내며 진동이 0.30 → 1.90 mm가 된다.
- **지표를 못 고르면 아무것도 안 보인다.** E_min은 선로 위 에너지에 가려 네 조건을 정렬조차 못 하고,
  최대 낙폭은 정렬하지만 **지연이 다른 조건끼리는 비교가 안 된다**(선로가 길면 떠 있는 에너지가 크다).
  평균으로 채점하면 연집성은 아예 보이지 않는다 — 52·53번의 최댓값↔RMS 문제가 세 번째로 나온다.

**58번은 57번이 "미해결"로 남긴 것을 설계해 닫는다 — 틀린 진단을 기록만 하지 않고 보완하는 절차다.**
- **먼저 "지금의 안전이 우연인가"를 절제로 가른다.** 조직 반력·표류 보정·국소 감쇠를 하나씩 빼고
  같은 암전에서 재보면 맹행이 **2.1 ~ 28.9 mm**로 벌어진다. 어느 항이 남아 있느냐에 달려 있으니
  **설계가 아니라 이 플랜트가 우연히 갖고 있던 성질**이었다. (안전 성질에도 절제를 쓴 것 — 9번 교훈의
  대상이 성능에서 안전으로 옮겨온 자리다.)
- **정지를 설계한다. 두 가지를 지킨다.** ① 판정은 **새 표본 없이 움직인 누적 거리**가 사슬이 이미
  선언한 여유(45번 shaft 2.17 mm, 48번 통로 1.25 mm 계열)를 넘을 때 — **통신 임계값이 아니라
  해부에서 오는 숫자**다. ② 집행은 **국소**다: 팔이 자기 위치를 스프링-댐퍼로 붙든다. 채널을 안 거치니
  채널이 죽어도 동작하고, 고정점을 향한 소산이라 에너지를 만들지 않는다(50번 "벽은 로컬 렌더링"을
  실패 경로에 적용).
- **결과는 평균이 아니라 흔들림으로 읽는다**: 2.1~28.9 → **1.2~2.8 mm**. 경계가 선언한 값에서 온다.
  그리고 과제는 계속 완주한다(50.8/55 mm) — 56번의 0 채움처럼 "안전한데 못 쓰는" 쪽으로 안 갔다.
- **그러고 나서야 57번의 실패한 처방이 된다**: λ 게이팅이 57번에서 4.72 → 6.53 mm로 악화됐는데,
  정지를 먼저 넣으면 1.91 → **1.68 mm**로 해가 없다. **대체 먼저, 억제 나중**을 숫자로 확인한 것.
- **대가는 두 개고 둘 다 알고리즘 밖에서 온다.** 선언한 여유가 **정지 빈도를 산다**(0.5 mm면 시간의
  56%를 멈춰 있고 4.0 mm면 12%). 그리고 **복귀가 가장 위험하다** — 즉시 복귀는 155 mm/s로 튀어나가고
  램프 200 ms면 ~90 mm/s인데 정지 구간이 9% → 73%가 된다(47번 관통 돌진과 같은 모양).
**59번은 58번이 남긴 두 자백을 물으려다 두 번 막히고, 막힌 이유가 결과가 된 실험이다.**
- **"붙들고 있으면 위험한가"를 물으려면 모델을 먼저 고쳐야 했다.** 정지 중에 환자가 움직이게 넣었는데
  힘이 거의 안 변한다 — 사슬이 47번 이후 쓴 조직 모델은 관통 후 **절삭력 + 마찰**뿐이라 **탄성이 없다.**
  조직 모델만 떼어내 도구를 고정하고 표면만 흔들어 보면 5 mm 왕복에 힘 변동이 **0.12 N**이다.
  바늘-조직 마찰의 표준 모형인 **stick-slip 파악**을 넣으면 같은 조건에서 **1.62 N — 14배.**
  → **모델이 표현하지 못하는 위해는 정책으로 비교할 수 없다. '차이 없음'은 결과가 아니라 침묵이다.**
- **그런데 물어보니 후퇴가 이기지 않는다**(정직한 네거티브). 파악 항의 몫은 미끄러짐 한계에서 포화하고
  절삭 기저(2~3 N)에 비해 작다. 후퇴는 조직에 얹는 몫을 1.68 → 0.89 N로 줄이지만 맹행을 **3.6배**로
  늘린다. 뒤집힐 조건은 명확하다 — **어느 조직인가가 정책을 고른다.**
- **술자 쪽에서는 대책의 부호가 뒤집힌다.** 마스터를 잠그면 어긋남은 줄지만 복귀가 **나빠진다**
  (120 → 133 mm/s): 잠금이 의도를 없애는 게 아니라 손 스프링에 저장하고, 더 나쁘게는 **술자가 반응해야
  할 그 신호를 가린다.** "도구가 뒤처지면 반응시간 뒤에 손을 멈춘다" 규칙 하나가 복귀 중앙값을
  120 → 68 mm/s로 내린다. **술자 쪽 대책은 술자 모델이 적응적일 때만 평가할 수 있다.**
- **여기서 또 통계 실수를 잡았다**: 중앙값 대 중앙값(1.8배)이 짝지은 효과를 과장한다. 같은 시드끼리
  비교하면 감소율 중앙값 25%, 개선된 시드 11/16이다 — 52번의 검출률↔AUROC와 같은 종류다.

- **설계 중 밟은 함정 셋**(테스트로 못박아 뒀다): ① β=0 순간을 트리거로 쓰니 정지가 98.5% 걸렸다
  — 순간 상태가 아니라 **그 상태로 얼마나 갔는지**를 세야 한다 ② 해제에 연속 fresh 스텝을 요구하니
  지터 채널에서 영구히 래치됐다 — 정보가 오는 스텝에서만 램프를 올리면 되고, 부수적으로 **링크가
  나쁠수록 복귀가 느려지는** 좋은 성질이 붙는다 ③ 복귀 속도를 램프 구간에서만 재니 즉시 복귀가
  가장 얌전해 보였다 — 고정 창에서 재야 한다.

**60번 — 그 측정이 결정을 가르기는 하는가** (`60_measure_to_decide.py`)
59번은 "이제 조직 값을 재러 가자"로 끝났다. **가기 전에 그 측정이 결정을 바꾸는지부터 계산했고,
답은 아니오였다.** 이건 실험 실패가 아니라 실험을 아낀 것이다.
- **정보의 값이 0이다.** 조직에 얹는 힘과 맹행을 맞바꾸는 **교환비(mm/N)** 는 임상이 정하는 값이라
  스윕했는데, 맹행이 비싼 쪽 전체에서 미끄러짐 한계 0.4~6.4 N 어디서나 결론이 **붙들기**로 같다.
  구속변수는 조직이 아니라 교환비였고, 그건 재는 게 아니라 **받아야 하는 선언**이다
  (58번의 '선언된 여유', 57번의 '허용 지연'과 같은 자리).
- **59번이 적어둔 뒤집힘 조건이 틀렸다.** "미끄러짐 한계가 크면 후퇴가 이긴다"고 썼는데 반대다 —
  **후퇴도 같은 파악 항을 거슬러 끌기** 때문이다(끌리는 힘이 정확히 F_slip). 붙들기 쪽만 보고
  추론한 결과였다.
- **못 재는 이유와 안 중요한 이유가 같다.** 파악 항은 `K_grip × 상대 운동`에서 포화한다.
  F_slip 3.2 N과 6.4 N의 결과가 소수점까지 같고, 측정을 막는 천장도 정확히 같은 곱이다.
  **같은 양이 위해와 관측 가능성을 동시에 자른다.**
- **그래도 재야 할 날의 프로토콜**: 평범한 삽입 로그로는 안 된다 — 회귀 상수항이
  `F_cut + F_slip`과 **소수점까지 같은** 완전 교락이다(잡음이 아니라 정보가 **없는** 것이다).
  멈춰서(dwell) 조직이 움직여야 하고, 왕복 진폭이 `2·F_slip/K_grip`을 넘어야 한다. 그 값이 참값에
  달려 있어 미리 못 고르는데, **진폭 사다리를 올려 값이 안 자라면 거기가 참값**이다 —
  **참값을 몰라도 수렴은 알 수 있다**(46번의 "모델 확장과 여기 설계는 한 수"가 조직에서 재현됐다).
  삽입 구간은 버리지 않는다: **기울기**가 깨끗한 마찰 추정이라 dwell 보정에 쓴다.
- **가장 위험한 건 잡음이 아니라 그럴듯한 오답이다.** 호흡만으로 재면 어떤 조직이든
  `K_grip × A / 2 = 1.50 N`이 나온다 — 조직이 아니라 **호흡 진폭을 잰 것**이다.
- **함정 넷**(테스트로 고정): ① 여기 주파수를 호흡과 같게 잡아 상대 운동이 **정확히 상쇄**됐다
  ② 관통 전 비선형 구간을 회귀에 넣어 상수항이 무의미해졌다 ③ 고리 모양 하나로 포화를 판정하려다
  되붙음 리플에 속았다 ④ 위해 지표를 정지 시점 대비 증분으로 잡으니 **정지 위상**에 휘둘려
  환자가 더 움직일수록 부하가 작아지는 착시가 나왔다.

**61번 — 조직은 이완한다** (`61_tissue_relaxes.py`)
60번이 프로토콜과 판정을 내놓으면서 각각에 "이완이 있으면 다르다"를 한계로 적었다. 그 물리를 실제로
넣어 **자기가 방금 쓴 것**을 시험했다. **예측을 둘 했고 둘 다 틀렸는데, 그게 이 실험의 내용이다.**
- **예측 ① "누적 지표로 바꾸면 60번이 뒤집힌다" → 틀렸다.** 창의 위치와 단위(mm/N → mm/(N·s))만
  바뀌고 구조는 같다. 구속변수는 여전히 **선언된 교환비**다.
- **예측 ② "진폭 사다리가 틀린 값에서 수렴한다" → 틀렸다. 구조적으로 불가능하다.** 고정 주파수에서
  진폭을 올리면 **속도도 같이 오르므로**(v = A·ω) 점성 천장 `K·v·τ` 도 기하학적 천장 `K·A` 도
  둘 다 사다리를 따라 올라간다. **F_slip 자신을 뺀 모든 천장이 함께 올라가니 가짜 평탄부가 못 생긴다.**
  60번의 판정은 그때는 몰랐던 이유로 이완까지 덮고 있었다.
- **대신 지표 결함 둘을 잡았다.** ① **60번의 진폭 지표는 조직이 아니라 정지 제어기를 재고 있었다** —
  환자를 **완전히 세워도** 2.17 N 이 나오고 조직·τ 를 바꿔도 똑같다(정지 제어기가 목표로 정착하는
  과도현상). 대조군 하나면 드러나는 건데 60번은 그걸 안 돌렸다. ② 진폭을 **실행 전체에 걸쳐** 누적해서,
  관통을 걸친 정지가 하나만 있어도 스윙이 `F_PUNC` 로 찍혔다(시드 절반이 정확히 4.00 N).
  → 고치니 60번의 발표 숫자가 움직인다(8 → 15 mm/N, 6 → 13/15). **결론은 유지되고 오히려 세졌다.**
- **진짜로 깨진 것은 하나**: 삽입 상수항이 `F_cut + min(F_slip, K·v·τ)` 라 **느리게 넣으면 조직이
  아니라 삽입 속도를 잰다.** 교락 상대가 절삭력 + 속도로 늘었고, 두 오답이 똑같이 그럴듯하다.
- 이완이 무는 대가는 값이 아니라 **비용**이다 — 필요 진폭이 부푼다(순탄성 20 mm → τ=0.2·0.11 Hz 에서
  60 mm 로도 미해결). 싼 해법은 주파수를 올리는 것(1.1 Hz 에서 40 mm). 요구가 **속도** 사양으로 합쳐진다:
  `A > 2·F_slip/K_grip` **그리고** `A·ω > F_slip/(K_grip·τ)`.

**이 단계에서 배우는 가장 중요한 것 — 오차 예산(error budget)**
부품 지표(IK 반복수, 관절 추종 오차, FRE)는 전부 좋아 보일 수 있다. 환자에게 가는 건 그
**합**이다. 42번은 다섯 조건을 같은 지표로 비교해서, 엔드투엔드 오차가 16.4 mm → 0.09 mm로
줄어드는 과정과 **어느 항이 지배적인지가 상황마다 뒤집히는 것**을 보여준다(보정된 팔에서는
정합이 전부, 도구를 바꿔 3% 무게 오차가 생기면 서보가 정합의 10배). 개선할 곳을 직관으로
고르면 틀린다는 뜻이다.

---

### 8단계를 다 봤다면 — 안전공학 관점으로 한 번 더
같은 사슬을 "무엇이 잘못될 수 있고, 무엇이 그것을 막고, 그 증거가 어디 있는가"로 다시 읽은 문서가
[VERIFICATION.md](VERIFICATION.md) 다. 요구사항 26개 · 위험 35개 · 각 위험의 완화와 **그것을 증명하는
테스트 이름**까지 연결돼 있다. 의료·안전 필수 분야에서는 알고리즘이 절반이고 이쪽이 나머지 절반이라,
공부하는 입장에서도 "테스트를 왜 그렇게 썼는지"가 이 문서를 보면 이해된다.

## 이 저장소가 반복해서 가르치는 열다섯 가지

번호 순서로 읽으면 같은 교훈이 다른 옷을 입고 계속 돌아온다. 이게 사실 실험 목록보다 중요하다.

1. **관측되지 않는 것은 추정되지 않는다.** IMU 바이어스(4), SLAM 헤딩(5), 자세 추정(37),
   팔의 마찰(43·46), 그리고 표면에 자취를 남기지 않는 심부 변형 모드(52)에서 다섯 번 반복된다.
   데이터가 그 값을 흔들어주지 않으면 어떤 추정기도 못 찾는다 — 그래서 "일부러 흔드는 궤적"을
   설계하고, 그래도 안 보이면 **다른 종류의 관측**을 가져온다.
2. **불확실도는 결과의 일부다.** 추정기는 값만이 아니라 **얼마나 못 믿는지**도 준다. 그
   불확실도를 안전 마진으로 쓰면(9·42) 위험한 동작을 미리 멈출 수 있다.
3. **부품 최적화 ≠ 시스템 최적화.** 디노이저가 SNR을 올리면서 진단을 해치고(다른 저장소),
   서보를 아무리 정밀하게 해도 정합이 틀리면 소용없다(42).
4. **합성에서 되는 것이 실데이터에서 되는 것은 아니다.** 안전 게이트가 합성 해부에서
   85%를 잡다가 실제 레이저 스캔에서 5%로 붕괴한다(44). 실패의 *성질*이 바뀌기 때문이다.
5. **축소 모델은 질문을 사라지게 한다.** 도구를 점으로 보면(평면 모델) "몸통이 혈관을
   관통하는 각도"라는 질문 자체가 존재하지 않는다(45·48).
6. **검증은 두 번 한다.** 6축 동역학을 두 방식으로 독립 구현해 1e-10까지 맞춰본 뒤에야
   믿었고(45), 바늘 휨은 해석해와 대조해서 버그를 찾았다(48). 둘 다 눈으로는 안 잡히는
   종류였다.
7. **정규화는 개선이 아니라 가정을 사는 것이다.** 데이터가 부족한 곳에서 답을 내려면 반드시
   무언가를 가정해야 하고, 결과의 품질은 그 가정이 맞느냐로 갈린다(51). 좁은 노출에서 5.5배를
   벌어준 게 알고리즘이 아니라 가정 하나였고, 같은 자리에서 "물리 기반" 모델은 격자를
   촘촘히 할수록 나빠졌다. **더 정확히 푸는 것과 옳은 것을 푸는 것은 다르다.**
   그리고 그 가정이 맞는지는 **가정이 적용되는 데이터로는 확인할 수 없다**(52) — 확인하려면
   그 실패 모드를 실제로 볼 수 있는 관측을 새로 가져와야 한다. 1번 교훈과 같은 말이다.
8. **오차의 종류를 구분하지 않으면 대책이 헛돈다.** 무작위 오차는 표본으로 줄지만 계통 오차는
   안 줄고(53: 관측 32배에도 압박 편향은 그대로, 그 몫이 13%→31%로 커진다), 편향을 겨냥한 대책은
   잡음 한계를 못 넘는다(같은 대책이 교정은 살리고 게이트는 못 살린다). **그리고 모든 정확도
   숫자에는 "어떤 센서로 잰 것인가"가 붙어 있다** — 52의 0.81이 53에서 0.61이 된 이유다.
9. **"주어진 것"을 하나씩 빼 보면 숫자가 바뀐다.** 51은 사전지식의 값을 매기고, 52는 그 가정이
   검증되지 않음을, 53은 그 검증조차 이상 센서였음을, 54는 폐루프 이득이 측정이 아니었음을,
   55는 대응이 공짜가 아니었음(2.6배)을, 56은 상수 지연 가정을 뺐다. **결과를 늘리는 것보다 자기
   결과의 유효 범위를 좁히는 쪽이 신뢰를 만든다** — 규제 도메인에서 실제로 요구되는 사고방식이다.
10. **통과한 시험이 시스템에 대한 증거가 아닐 수 있다.** 56번에서 가정을 걷어냈는데 네 절 동안
    아무 일도 없었던 이유는 채널이 튼튼해서가 아니라 **그 설정이 과제를 완주하지 못해 채널을 여기
    못 했기** 때문이다(도구가 표적 55 mm 중 34.8 mm에서 멈춤). 완주시키자 같은 지터가 860배의
    에너지를 만들었다. 그리고 위반이 나온 자리는 수동성 증명이 **애초에 덮지 않는** 항이었다.
    → 로버스트니스 결과는 **과제 완주 여부와 함께** 읽어야 하고, 보증은 **그 보증이 덮는 블록**에서만
    유효하다. 8번(오차의 종류)과 같은 계열이지만 이번엔 **증거의 종류**에 관한 이야기다.
11. **보증 밖에 있는 항이 일을 하고 있을 수 있다.** 57번에서 56번이 "증명이 덮지 않는 결함"이라
    지목한 표류 보정 항을 예산으로 함께 죄어 보니 **더 나빠졌다**(맹행 4.72 → 6.53 mm). 그 항이
    통신이 끊긴 동안 도구를 붙잡는 브레이크였기 때문이다. **"보증 밖 항을 없애라"가 아니라 "그 항이
    조용히 하고 있는 일을 먼저 찾아 대체하라"**가 옳은 순서다. 그리고 57번은 내 예측이 틀린 실험이다 —
    56번을 닫으며 "이 대칭성을 깨면 결론이 좁아질 것"이라 적었는데 그렇지 않았고, **왜 안 그런지가
    예측보다 값이 나갔다.** 예측이 맞았는지보다 왜 그런지가 자산이다.
12. **틀린 진단은 기록으로 끝나면 안 된다 — 보완까지 해야 규칙이 된다.** 57번은 "보증 밖 항이
    브레이크였다"를 발견하고 규칙만 적었다. 58번이 그 규칙대로 **대체물을 만들어** 확인했다: 절제로
    "지금 경계는 우연"임을 증명하고(맹행이 구성에 따라 2.1~28.9 mm), **선언된 임상 여유**를 트리거로
    하고 **국소**로 집행하는 정지를 넣어 경계를 설계에서 오게 하고(1.2~2.8 mm), 그러고 나서 57번에서
    실패했던 억제를 다시 시험해 이제 해가 없음을 확인했다(1.91 → 1.68 mm).
    **발견 → 규칙 → 대체물 → 재시험**이 한 사이클이고, 마지막 단계까지 가지 않으면 규칙은 검증되지
    않은 채로 남는다. 그리고 이때 **트리거를 어디서 가져오는지가 설계의 질을 정한다** — 통신 임계값을
    튜닝하면 임의의 숫자가 되고, 이미 선언된 임상 여유에서 가져오면 시술마다 자동으로 맞는 값이 된다.
13. **정책을 비교하기 전에 "이 모델이 그 위해를 표현하는가"를 물어야 한다.** 59번에서 "붙들기 vs 후퇴"가
    구분되지 않은 것은 두 정책이 같아서가 아니라 조직 모델에 관통 후 탄성이 없어서였다(힘 변동 0.12 vs
    1.62 N, 14배). **표현력 없는 모델에서의 '차이 없음'은 결과가 아니라 침묵이다.** 같은 실험에서
    마스터 잠금이 나빠 보인 것도 잠금이 나빠서가 아니라 **술자 모델이 멈출 수 없어서**였다 —
    50번이 한계로 적어둔 항목이 결론의 **부호**를 바꿨다.
    이건 1번 교훈("관측되지 않는 것은 추정되지 않는다")의 짝이다: **표현되지 않는 것은 비교되지 않는다.**
14. **측정을 하러 가기 전에 그 측정의 값을 계산해야 한다.** 59번은 "조직 값을 재러 가자"로 끝났는데,
    60번이 먼저 계산해 보니 **그럴듯한 임상 교환비 범위 어디서도 결론이 안 바뀌었다.** 구속변수는
    조직이 아니라 **교환비**였고, 그건 측정이 아니라 선언이다. 그리고 못 재는 이유와 안 중요한 이유가
    같았다 — 파악 항이 `K_grip × 상대 운동`에서 포화하므로 그 위의 값은 **해롭지도 않고 보이지도
    않는다.** 재기로 했다면 두 가지가 더 있다: 평범한 로그에서 그 값이 **교락**되어 있지 않은지
    (여기서는 상수항이 `F_cut + F_slip`과 소수점까지 같았다 — 잡음이 아니라 정보의 부재다), 그리고
    필요한 **여기**가 참값에 달려 있을 때 어떻게 푸는지(**사다리를 올려 값이 안 자라면 거기다** —
    참값을 몰라도 수렴은 안다). 앞의 교훈들이 "무엇을 재야 하는가"였다면 이건 **"재지 않아도 되는
    것을 어떻게 알아보는가"** 다.
15. **지표가 그 위해에 반응하는지부터 확인해야 한다 — 위해를 끈 대조군으로.** 61번이 60번의 진폭
    지표를 환자를 **완전히 세운 채** 재보니 2.17 N 이 나왔고, 조직을 바꿔도 τ 를 바꿔도 같았다.
    그건 조직이 아니라 **정지 제어기의 정착 과도현상**이었다. 대조군 한 번이면 드러나는데 60번은
    안 돌렸다. 여기서 지표가 **세 번 연달아** 바뀐 것도 봐 둘 만하다 — 증분(위상에 민감) → 진폭
    (과도현상이 지배) → 누적. **한 결함을 고치는 교체가 다른 결함을 들여온다.** 그래서 교체할
    때마다 "이 지표가 위해에 반응하는가"를 위해가 **없는** 조건에서 다시 물어야 한다.
    (13번이 "모델이 위해를 표현하는가"였다면, 이건 **"지표가 위해를 보는가"** 다.)
    그리고 정직하게: 여기 지표는 전부 **힘**에서 나왔다. 이완 조직은 힘이 사라져도 **변형이 남으므로**,
    손상이 변형·허혈 시간이라면 이 저장소의 어떤 지표도 그걸 못 본다.

---

## 정직한 한계 (공부할 때 같이 알아야 할 것)

- **데이터는 공개·합성이다.** 실제 환자·실제 수술 데이터가 아니다. 실측 앵커는 공개 데이터
  (표준 g2o 벤치마크, Stanford Bunny 스캔, 공개 인체 MR 볼륨)와 공개 로봇 사양(UR5)까지다.
- **51~55번의 변형장은 합성이다.** 심부 표적의 정답을 알아야 오차를 잴 수 있어서 해석적 장을
  썼다. 그래서 절대값이 아니라 **방법 간 상대 비교**로 읽어야 한다.
- **53번의 초음파도 모델이다.** 압박·잡음·오대응을 넣어 52번보다는 정직해졌지만, 조직 점탄성
  (누른 뒤 시간에 따라 이완)과 초음파의 방향 이방성은 여전히 없다. 즉 53번의 숫자도 여전히
  낙관적이며, 요점은 "이 값이 맞다"가 아니라 **"이상 센서 가정이 값을 얼마나 부풀리는가"** 다.
- **"실기"도 시뮬레이션이다.** 43번의 sim-to-real 루프는 파라미터와 누락 항을 일부러
  어긋나게 둔 시뮬이며, 백래시·관절 탄성 같은 실물 현상은 없다.
- **의료 부분은 원리 이식의 데모다.** 임상 검증·규제 승인과는 다른 층위의 이야기다.

---

## 더 깊이 가려면 (표준 교재)

이 저장소는 "직접 구현해 보는" 쪽이라, 이론의 체계는 교재로 채우는 편이 좋다.
- *Probabilistic Robotics* (Thrun, Burgard, Fox) — 필터·SLAM·측위의 표준 교재
- *Modern Robotics* (Lynch & Park) — 기구학·동역학·제어를 현대적 표기로
- *Robot Modeling and Control* (Spong, Hutchinson, Vidyasagar) — 매니퓰레이터 동역학·제어
- 이 저장소의 [블로그 시리즈 7편](blog/00_index.md) — 실험들을 서사로 엮은 한국어 글

---

## 요약 표 — 61개 실험 지도

| 단계 | 주제 | 실험 번호 |
|---|---|---|
| 1 | 칼만 필터, 센서 융합 | 1, 2 |
| 2 | 비선형 필터(EKF/UKF), 바이어스, 자세추정 | 3, 4, 37 |
| 3 | VIO(시각-관성 융합) | 8, 10 |
| 4 | SLAM: 필터·그래프·루프클로저·3D·BA | 5, 6, 7, 12, 13, 17, 18, 35, 36 |
| 5 | 실데이터·강건성·라이다·매핑 | 11, 14, 15, 21, 23, 25, 29 |
| 6 | 계획과 제어 | 19, 20, 24, 26, 28 |
| 7 | 학습·sim-to-real·시스템식별 | 16, 30, 31, 32, 33, 34, 38 |
| 8 | 의료 로봇 사슬(기구학→정합→예산→접촉→유연바늘→실 인체 MR→원격조작→변형 정합→가정 검증→모달리티 현실화→휨 보상 폐루프→대응 탐색→지터·손실 채널→연집·긴꼬리→통신상실 정지→안전 상태란 무엇인가→그 측정은 할 값어치가 있나→조직은 이완한다) | 9, 22, 27, 39–61 |

(각 실험의 결과 숫자와 그림은 [README](README.md) 의 실험별 절에 정리돼 있다.)
